// Copy this file to secrets.h and fill in real values.
// secrets.h is gitignored and will not be committed.
#pragma once

// Shared secret for authenticating with Worker endpoints (/ingest).
// MUST match the CMD_SECRET set via `wrangler secret put CMD_SECRET`.
// Value: first 32 hex chars of SHA-256("cmd:<your dashboard password>")
#define CMD_SECRET "CHANGE_ME_TO_MATCH_WORKER"
